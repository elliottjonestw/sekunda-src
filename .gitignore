# Logs
logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log*

node_modules
dist
dist-ssr
*.local

# Cloudflare Worker
# .wrangler holds the local Miniflare D1 state — a disposable dev database that
# is rebuilt by `npm run worker:migrate`.
.wrangler
# .dev.vars is the local stand-in for `wrangler secret put`. It holds real
# signing keys; never commit it. See worker/.dev.vars.example.
.dev.vars
worker/worker-configuration.d.ts

# QA test-account credentials (password + API key). Local only — never commit.
QA_ACCESS.md

# Editor directories and files
.vscode/*
!.vscode/extensions.json
.idea
.DS_Store
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?
